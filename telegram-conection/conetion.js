
//Trazer os seletores para conectar no bot do telegram. 


/*var url = window.location.href;
var ver = 'https://livecasino.bet365.com/home/br';
if(url.indexOf(ver) != -1){
	function createCookie(name, value, days) {
		var expires;
		if (days) {
			var date = new Date();
			date.setTime(date.getTime() + (days * 24 * 60 * 60 * 1000));
			expires = "; expires=" + date.toGMTString();
		}
		else {
			expires = "";
		}
		document.cookie = name + "=" + value + expires + "; path=/";
	}

	function getCookie(c_name) {
		if (document.cookie.length > 0) {
			c_start = document.cookie.indexOf(c_name + "=");
			if (c_start != -1) {
				c_start = c_start + c_name.length + 1;
				c_end = document.cookie.indexOf(";", c_start);
				if (c_end == -1) {
					c_end = document.cookie.length;
				}
				return unescape(document.cookie.substring(c_start, c_end));
			}
		}
		return "";
	}
	function msgT(msg){
		let token   = "5164370647:AAERKf6tGErqifX0COtQJ-bfU6l4tyYO110";// TOKEN DO BOT QUE ENVIARA AS MENSAGENS
		let chat_id = "-1001663429936"; // CHAT ID DO GRUPO/CANAL QUE VAI AS MENSAGENS
		let text     = msg;
		fetch('https://api.telegram.org/bot' + token + '/sendMessage?chat_id=' + chat_id + '&text=' + encodeURIComponent(text) + '&parse_mode=html&disable_web_page_preview=True', {
		method: 'GET'
		})
		.then(response => response.json())
	}}


	*/

